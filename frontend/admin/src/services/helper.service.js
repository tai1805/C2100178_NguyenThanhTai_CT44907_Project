export function configFormData() {
    const config = {
        headers: {
            "Content-Type": "multipart/form-data",
            Accept: "multipart/form-data"
        }
    };
    return config; 
}